# Base URL of the FastAPI backend. Locally this is uvicorn's default;
# in production, set this to the deployed Render/Railway backend URL
# in the Vercel project's environment variables.
NEXT_PUBLIC_API_URL=http://127.0.0.1:8000